# Sanitized protocol manifests

`protocols.json` contains the experiment-level settings needed to interpret the public results. Machine-specific service names, local paths, ports and fleet scheduling controls are intentionally not exported. Runtime/model revisions live beside their corresponding results.
